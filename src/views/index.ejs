<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="min-h-screen bg-gray-100 p-5">
    <div class="max-w-2xl mx-auto bg-white rounded-lg shadow-md p-8">
        <h1 class="text-3xl font-bold text-primary text-center mb-8">Daily Inspiration</h1>
        
        <% if (saying) { %>
            <div class="bg-gray-50 p-8 rounded-lg mb-8 text-center">
                <blockquote class="text-xl italic m-0 p-0">
                    "<%= saying.text %>"
                    <% if (saying.author) { %>
                        <footer class="mt-4 text-base text-primary">— <%= saying.author %></footer>
                    <% } %>
                </blockquote>
            </div>
        <% } else { %>
            <p class="text-center text-primary italic">No saying for today yet. Check back later!</p>
        <% } %>

        <div class="text-center">
            <h2 class="text-2xl font-semibold text-primary mb-4">Get Daily Inspiration by Email</h2>
            <form id="subscribeForm" class="space-y-4 max-w-sm mx-auto">
                <div>
                    <input type="email" name="email" class="w-full p-2 border border-gray-300 rounded text-base" placeholder="Enter your email" required>
                </div>
                <div>
                    <label for="preferredEmailHour" class="block text-sm font-medium text-gray-700 mb-1">Preferred email time (your local time)</label>
                    <select name="preferredEmailHour" id="preferredEmailHour" class="w-full p-2 border border-gray-300 rounded text-base" required>
                        <% for(let i = 0; i < 24; i++) { %>
                            <option value="<%= i %>" <%= i === 8 ? 'selected' : '' %>><%= i.toString().padStart(2, '0') %>:00</option>
                        <% } %>
                    </select>
                </div>
                <button type="submit" class="w-full px-4 py-2 bg-secondary text-white rounded hover:bg-blue-600 transition duration-300">Subscribe</button>
            </form>
            <div id="subscriptionMessage" class="mt-4 p-2 rounded"></div>
        </div>
    </div>

    <script src="/js/subscribe.js"></script>
</body>
</html> 